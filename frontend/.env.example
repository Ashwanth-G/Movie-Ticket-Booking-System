# API base URL (leave empty when using Vite proxy in dev)
# In production, set to your backend URL, e.g. https://api.example.com
VITE_API_URL=

# Optional: Company logo URL for navbar (top left, next to "Movie Booking")
# e.g. https://yoursite.com/logo.png or /logo.png
VITE_LOGO_URL=
